@model Proyecto.Models.RolesSistema

@{
    ViewData["Title"] = "Detalles";
}

<style>
    body {
        background: linear-gradient(135deg, #ffecd2, #fcb69f);
        font-family: "Poppins", sans-serif;
        margin: 0;
        padding: 20px;
    }

    h1 {
        text-align: center;
        font-size: 2.5rem;
        color: #fff;
        font-weight: 800;
        margin-bottom: 10px;
        text-shadow: 2px 2px 8px rgba(0,0,0,0.2);
    }

    h4 {
        text-align: center;
        color: #6a5af9;
        margin-bottom: 10px;
        font-size: 1.4rem;
        font-weight: 700;
    }

    .school-card {
        background: white;
        border-radius: 25px;
        padding: 30px;
        max-width: 600px;
        margin: auto;
        border: 4px dashed #ff7a5a;
        box-shadow: 0 8px 18px rgba(0,0,0,0.15);
        animation: pop 0.6s ease;
    }

    @@keyframes pop {
        0% {
            transform: scale(0.95);
            opacity: 0;
        }

        100% {
            transform: scale(1);
            opacity: 1;
        }
    }

    dt {
        font-weight: 700;
        color: #6a5af9;
        margin-top: 10px;
        font-size: 1.1rem;
    }

    dd {
        background: #fff3e0;
        border-radius: 12px;
        padding: 10px;
        margin-bottom: 10px;
        border-left: 6px solid #ffb85f;
        font-size: 1rem;
        font-weight: 600;
        color: #444;
    }

    .btn-edit {
        display: inline-block;
        background: #6a5af9;
        color: white;
        padding: 12px 20px;
        border-radius: 18px;
        text-decoration: none;
        font-weight: 700;
        margin-top: 10px;
        width: 100%;
        text-align: center;
        box-shadow: 0 5px 12px rgba(106, 90, 249, 0.4);
        transition: 0.2s ease-in-out;
    }

        .btn-edit:hover {
            transform: scale(1.06);
            filter: brightness(1.1);
        }

    .back-link {
        display: block;
        margin-top: 15px;
        text-align: center;
        font-weight: 700;
        color: white;
        font-size: 1.1rem;
        text-shadow: 1px 1px 4px rgba(0,0,0,0.2);
        text-decoration: none;
    }

        .back-link:hover {
            text-decoration: underline;
        }
</style>

<h1>📘 Detalles del Rol</h1>

<div class="school-card">
    <h4>Información del Rol</h4>
    <hr />

    <dl class="row">
        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nombre)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Nombre)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Apellido1)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Apellido1)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Apellido2)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Apellido2)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Contraseña)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Contraseña)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Correo)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Correo)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.IdRolNavigation)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.IdRolNavigation.IdRol)</dd>
    </dl>

    <a class="btn-edit" asp-action="Edit" asp-route-id="@Model?.Identificacion">✏ Editar</a>
</div>

<a class="back-link" asp-action="Index">⬅ Volver al listado</a>
